import React from 'react';

//import { graphql } from 'apollo-client';

//import profileOverviewQuery from '../../queries/profileOverview';

const ProfileOverview = (props) => {

  return (
    <h2>Overview</h2>
  )
}

export default ProfileOverview;
